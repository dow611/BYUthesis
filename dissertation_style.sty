\usepackage{lipsum} % generate filler text
\usepackage[pdfusetitle]{hyperref}
\makeatletter % necessary to use private @author
 \hypersetup{
  pdfstartview = {FitH},
  bookmarksnumbered  = true, % section numbers in bookmarks
  bookmarksopen = true, % open up the bookmark trees
  bookmarksopenlevel = 1, % level to which bookmarks are open
  pdfpagemode = {UseOutlines}, % specify how document opens
  pdfcreator = {\@author}, % autofill author
  pdfauthor= {\@author}% autofill author
  }
\makeatother % finishes use of private @author
